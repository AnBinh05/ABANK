# 📱 File APK cần tải về điện thoại

## ✅ File bạn cần tải:

**`app-debug.apk`**

## 📍 Vị trí file APK:

Có 2 nơi có thể có file APK:

### 1. Thư mục chính (sau khi build xong):
```
D:\ABANK\app\build\outputs\apk\debug\app-debug.apk
```

### 2. Thư mục tạm (nếu chưa build đầy đủ):
```
D:\ABANK\app\build\intermediates\apk\debug\app-debug.apk
```

## 🔍 Cách tìm file APK:

### Cách 1: Trong Android Studio (Dễ nhất)
1. Build APK: `Build > Build Bundle(s) / APK(s) > Build APK(s)`
2. Khi build xong, nhấn **`locate`** trong popup
3. File Explorer sẽ tự động mở thư mục chứa APK

### Cách 2: Dùng File Explorer
1. Mở File Explorer (Windows)
2. Điều hướng đến: `D:\ABANK\app\build\outputs\apk\debug\`
3. Tìm file: **`app-debug.apk`**

### Cách 3: Tìm kiếm
1. Mở File Explorer
2. Vào thư mục `D:\ABANK`
3. Tìm kiếm: `*.apk`
4. Chọn file `app-debug.apk`

## 📦 Thông tin file APK:

- **Tên file**: `app-debug.apk`
- **Kích thước**: Khoảng 5-10 MB
- **Loại**: Debug APK (dùng để test)
- **Có thể cài đặt**: ✅ Có

## 📲 Cách tải file APK lên điện thoại:

### Cách 1: Qua USB (Nhanh nhất)
1. Kết nối điện thoại với máy tính qua USB
2. Copy file `app-debug.apk` vào thư mục **Download** hoặc **Documents** trên điện thoại
3. Ngắt kết nối USB
4. Mở File Manager trên điện thoại
5. Tìm file `app-debug.apk` và nhấn vào để cài đặt

### Cách 2: Qua Email
1. Gửi file `app-debug.apk` vào email của chính bạn
2. Mở email trên điện thoại
3. Tải file đính kèm về
4. Mở file APK và cài đặt

### Cách 3: Qua Google Drive / Dropbox
1. Upload file `app-debug.apk` lên Google Drive hoặc Dropbox
2. Mở Google Drive/Dropbox trên điện thoại
3. Tải file về
4. Mở file APK và cài đặt

### Cách 4: Qua Bluetooth
1. Bật Bluetooth trên cả máy tính và điện thoại
2. Gửi file `app-debug.apk` qua Bluetooth
3. Chấp nhận file trên điện thoại
4. Mở file APK và cài đặt

### Cách 5: Qua ADB (Nếu điện thoại đang kết nối USB)
```bash
adb install app/build/outputs/apk/debug/app-debug.apk
```
hoặc
```bash
adb install app/build/intermediates/apk/debug/app-debug.apk
```

## ⚠️ Lưu ý khi cài đặt:

1. **Cho phép cài đặt từ nguồn không xác định**:
   - Khi mở file APK, điện thoại có thể hỏi "Cài đặt từ nguồn không xác định"
   - Chọn **"Cho phép"** hoặc **"Allow"**
   - Hoặc vào Settings > Security > Cho phép cài đặt từ nguồn không xác định

2. **Android version**:
   - Đảm bảo điện thoại chạy Android 8.0 (API 26) trở lên

3. **Gỡ app cũ** (nếu có):
   - Nếu đã cài app trước đó, nên gỡ ra trước khi cài lại

## ✅ Sau khi cài đặt:

1. Mở app **Bank Notification Reader**
2. Nhấn **"Bật quyền ngay"**
3. Bật quyền **Notification Access**
4. Quay lại app và sử dụng!

---

## 🎯 Tóm tắt:

**File cần tải**: `app-debug.apk`  
**Vị trí**: `D:\ABANK\app\build\outputs\apk\debug\` hoặc `D:\ABANK\app\build\intermediates\apk\debug\`  
**Cách tải**: Copy qua USB, Email, Cloud, hoặc Bluetooth  
**Cài đặt**: Mở file APK trên điện thoại và cho phép cài đặt

---

**Chúc bạn cài đặt thành công! 🎉**




